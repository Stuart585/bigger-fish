export interface catchType {
    name: string
    species: string
    length: number
    date: string
    location: string
  }

  export interface LabelValue {
    label: string,
    value: string
  }